export type Monster = {
	name: string;
	age: number;
	tentacles: number;
	eyes: number;
	hasWings: boolean;
};

export const numberOfMonstersWithWings = (m) => {
	return 0;
};

export const getAllNoWingedMonster = (m) => {
	return [];
};
